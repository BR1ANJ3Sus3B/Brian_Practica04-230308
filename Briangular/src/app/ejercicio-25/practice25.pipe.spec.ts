import { Practice25Pipe } from './practice25.pipe';

describe('Practice25Pipe', () => {
  it('create an instance', () => {
    const pipe = new Practice25Pipe();
    expect(pipe).toBeTruthy();
  });
});
